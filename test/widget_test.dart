// Placeholder smoke test. The admin app requires a live Supabase session
// to render anything past the login screen, so a meaningful widget test
// would need a mock SupabaseClient. Left empty for now; lint passes.

void main() {}
